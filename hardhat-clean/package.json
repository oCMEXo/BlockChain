{
  "name": "hardhat-test",
  "version": "1.0.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:lock": "npx hardhat run scripts/deployLock.js --network baseSepolia",
    "deploy:greeter": "npx hardhat run scripts/deployGreeter.js --network baseSepolia",
    "interact": "npx hardhat run scripts/interact.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.4.0",
    "hardhat": "^2.22.0"
  }
}
